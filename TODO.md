# TODO

## Blocked
- [ ] Set up GPG secrets on GitHub repo so the canary Action can run

## High Priority
- [ ] End-to-end test recovery flow (fresh phone: Scan QR → enter phrase → re-pair tag → done)
- [ ] Test "Pair Additional Tag" in Settings with a second RFID sticker
- [ ] Show loading spinner during canary push on HomeScreen
- [ ] Pull-to-refresh on Chain screen

## Medium
- [ ] Show "GitHub not configured" card on HomeScreen instead of empty state
- [ ] Add connectivity check before network calls (friendlier offline errors)
- [ ] Progress/error state when GitHub config is missing — link to Settings

## Stretch
- [ ] Notification when GitHub Action completes (webhook → push notification)
- [ ] Chain visualization (graph of hash-linked canaries)
- [ ] App shortcut or homescreen widget for faster daily NFC tap
